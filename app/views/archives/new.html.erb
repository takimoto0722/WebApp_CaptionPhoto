<%= form_with(model: @archive, local: true, html: { multipart: true }) do |f| %>
  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.label :caption %>
  <%= f.text_field :caption %>

  <%= f.submit "加工して保存" %>
<% end %>
