one:
  email:   user_one@example.com
  # Devise を使っているなら、暗号化パスワードも必要
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password123') %>

two:
  email:   user_two@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password456') %>
